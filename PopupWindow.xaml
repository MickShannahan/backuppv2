<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:backuppv2"
             xmlns:pages="clr-namespace:backuppv2.Components.Pages"
             x:Class="backuppv2.PopupWindow"
             Shell.NavBarIsVisible="False"
             Title="Empty">



  <BlazorWebView x:Name="blazorWebView"
                 HostPage="wwwroot/index.html">
    <BlazorWebView.RootComponents>
      <RootComponent Selector="#app"
                     ComponentType="{x:Type pages:Settings}"/>
    </BlazorWebView.RootComponents>
  </BlazorWebView>

  <!-- <CollectionView x:Name="ActionsList"
                  SelectionMode="Single">
    <CollectionView.ItemsSource>
      <x:Array Type="{x:Type x:String}">
        <x:String>Open</x:String>
        <x:String>Close</x:String>
        <x:String>Quit</x:String>
      </x:Array>
    </CollectionView.ItemsSource>
    <CollectionView.ItemTemplate>
      <DataTemplate>
        <Label Text="{Binding}"
               FontSize="24"
               Padding="10"/>
      </DataTemplate>
    </CollectionView.ItemTemplate>
  </CollectionView> -->

</ContentPage>
